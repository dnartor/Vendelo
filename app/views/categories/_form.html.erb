<%= form_with(model: category) do |form| %>
  <p><% @category.errors.full_messages.each do |error|%></p>
  <%=error%>
  <% end %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
