<div id='favorite'>
    <% if product.favorite %>

        <%= link_to  '❤️', favorite_path(product.id), data:{
            turbo_method: :delete,
        } if Current.user %>

    <% else %>

        <%= link_to  '🖤', favorites_path(product_id: product.id), data:{
            turbo_method: :post,
        } if Current.user %>

    <% end %>
</div>